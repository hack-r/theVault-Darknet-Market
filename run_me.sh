echo "Run the installers from the root directory. Read the .txt files for manual steps."
cp -rf install/* .
echo "Files copied."
echo "These installers were written for RPM-based systems like Fedora but are easily used with other linux distros by updating the package manager name (e.g. apt instead of dnf) and some package names."
